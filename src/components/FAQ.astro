---
import { faqs, withBase } from '../data/siteData';
---

<section id="faq" class="py-20 bg-neutral-950">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-4">Preguntas frecuentes</h2>
      <p class="text-xl text-gray-300 max-w-2xl mx-auto">
        Todo lo que necesitas saber para entrenar en Gimnasio Macará
      </p>
    </div>

    <div class="space-y-4">
      {faqs.map((faq, index) => (
        <details class="faq-item bg-neutral-900 rounded-xl border border-neutral-800 hover:border-yellow-400 transition-all">
          <summary class="flex items-center justify-between p-6 cursor-pointer list-none">
            <h3 class="text-xl font-bold text-white pr-4">{faq.question}</h3>
            <i class="faq-icon ri-arrow-down-s-line text-2xl text-yellow-400 transition-transform shrink-0"></i>
          </summary>
          <div class="px-6 pb-6">
            <p class="text-gray-300 leading-relaxed">{faq.answer}</p>
          </div>
        </details>
      ))}
    </div>

    <div class="text-center mt-12">
      <p class="text-gray-400 mb-4">¿Todavía tienes dudas?</p>
      <a
        href={withBase("/contact")}
        class="inline-flex items-center gap-3 bg-yellow-400 hover:bg-yellow-300 text-black px-8 py-4 rounded-lg font-bold text-lg transition-all transform hover:scale-105"
      >
        Contacto
        <i class="ri-arrow-right-line"></i>
      </a>
    </div>
  </div>
</section>

<style>
  details summary::-webkit-details-marker {
    display: none;
  }
  
  .faq-item[open] .faq-icon {
    transform: rotate(180deg);
  }
</style>

