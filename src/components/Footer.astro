---
import { siteData, navigation, socialLinks, contactInfo } from '../data/siteData';
---

<footer class="bg-neutral-900 border-t border-neutral-800">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      <!-- About Section -->
      <div>
        <h3 class="text-2xl font-bold text-white mb-4">{siteData.name}</h3>
        <p class="text-gray-400 mb-4">{siteData.description}</p>
        <div class="flex space-x-4">
          {socialLinks.map((social) => (
            <a
              href={siteData.social[social.name as keyof typeof siteData.social]}
              target="_blank"
              rel="noopener noreferrer"
              class="w-10 h-10 bg-neutral-950 hover:bg-yellow-400 text-gray-400 hover:text-white rounded-lg flex items-center justify-center transition-all"
              aria-label={social.label}
            >
              <i class={`${social.icon} text-xl`}></i>
            </a>
          ))}
        </div>
      </div>

      <!-- Enlaces -->
      <div>
        <h4 class="text-lg font-semibold text-white mb-4">Enlaces</h4>
        <ul class="space-y-2">
          {navigation.map((item) => (
            <li>
              <a
                href={item.href}
                class="text-gray-400 hover:text-yellow-400 transition-colors"
              >
                {item.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Contact Info -->
      <div>
        <h4 class="text-lg font-semibold text-white mb-4">Contacto</h4>
        <ul class="space-y-4 text-gray-400">
          {contactInfo.slice(0, 3).map((info) => (
            <li class="flex items-start gap-3">
              <i class={`${info.icon} text-yellow-400 text-xl mt-1 shrink-0`}></i>
              {info.link ? (
                <a href={info.link} class="hover:text-yellow-400 transition-colors leading-relaxed">
                  {info.content}
                </a>
              ) : (
                <span class="leading-relaxed">{info.content}</span>
              )}
            </li>
          ))}
        </ul>
      </div>

      <!-- Novedades -->
      <div>
        <h4 class="text-lg font-semibold text-white mb-4">Novedades</h4>
        <p class="text-gray-400 mb-4">Recibe novedades, horarios y promociones del gimnasio</p>
        <form class="flex flex-col space-y-2">
          <input
            type="email"
            placeholder="Tu correo"
            class="px-4 py-2 bg-neutral-950 border border-neutral-800 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:border-yellow-400"
          />
          <button
            type="submit"
            class="bg-yellow-400 hover:bg-yellow-300 text-black px-4 py-2 rounded-lg font-semibold transition-colors"
          >
            Suscribirme
          </button>
        </form>
      </div>
    </div>

    <div class="border-t border-neutral-800 mt-8 pt-8 text-center text-gray-400">
      <p>&copy; {new Date().getFullYear()} {siteData.name}. Todos los derechos reservados.</p>
    </div>
  </div>
</footer>

